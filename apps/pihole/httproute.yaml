apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: web-ui
  namespace: pihole
spec:
  parentRefs:
    - name: internal
      namespace: infrastructure
  hostnames:
    - pihole.cloud.theoraime.com
  rules:
    - backendRefs:
        - name: pihole-web
          port: 80
---
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: http-filter-redirect
  namespace: pocket-id
spec:
  parentRefs:
    - name: internal
      namespace: infrastructure
      sectionName: http
    - name: external
      namespace: infrastructure
      sectionName: http
  rules:
  - filters:
    - type: RequestRedirect
      requestRedirect:
        scheme: https
        statusCode: 301